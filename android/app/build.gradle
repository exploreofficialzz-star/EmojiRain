plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace "com.chastech.emojirain"
    compileSdk flutter.compileSdkVersion
    ndkVersion flutter.ndkVersion

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
        coreLibraryDesugaringEnabled true
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId "com.chastech.emojirain"
        minSdk 21
        targetSdk flutter.targetSdkVersion
        versionCode flutter.versionCode
        versionName flutter.versionName
        multiDexEnabled true
    }

    signingConfigs {
        release {
            keyAlias     System.getenv("KEY_ALIAS")     ?: ""
            keyPassword  System.getenv("KEY_PASSWORD")  ?: ""
            storeFile    System.getenv("STORE_FILE") ? file(System.getenv("STORE_FILE")) : null
            storePassword System.getenv("STORE_PASSWORD") ?: ""
        }
    }

    buildTypes {
        debug {
            applicationIdSuffix ".debug"
        }
        release {
            signingConfig signingConfigs.release
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"
        }
    }
}

flutter {
    source "../.."
}

dependencies {
    coreLibraryDesugaring "com.android.tools:desugar_jdk_libs:2.1.2"
    implementation "org.jetbrains.kotlin:kotlin-stdlib:2.1.0"
    implementation "androidx.multidex:multidex:2.0.1"

    // ── Google Play Billing Library 8.x ───────────────────────────────────
    // Google mandates Billing Library 8.0.0+ from August 31, 2026 — apps
    // that don't comply will have updates rejected.
    //
    // in_app_purchase_android 0.5.0 (pinned via dependency_overrides in
    // pubspec.yaml) already declares this natively. The explicit declaration
    // below is a belt-and-suspenders guarantee: Gradle always resolves to
    // the highest declared version, so this wins over any older transitive
    // billing dependency from other packages in the tree.
    implementation "com.android.billingclient:billing-ktx:8.3.0"

    // ── Unity Ads mediation adapter for AdMob ─────────────────────────────
    // Includes Unity Ads SDK as a transitive dependency (no separate SDK dep needed).
    // Configure mediation in AdMob console: Mediation → Create mediation group
    // → Add Unity Ads as ad source → map placement IDs to your AdMob ad units.
    //
    // Unity Ads placement IDs (from Unity dashboard):
    //   Game ID:      800079040
    //   Rewarded:     Rewarded_Android
    //   Interstitial: Interstitial_Android
    //   Banner:       Banner_Android
    implementation "com.google.ads.mediation:unity:4.12.4.0"
}
